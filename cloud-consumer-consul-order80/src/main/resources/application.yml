server:
  port: 80
spring:
  application:
    name: cloud-consumer-order
  cloud:
    consul:
      #consul注册中心地址
      host: localhost
      #consul注册中心地址
      port: 8500
      discovery:
        service-name: ${spring.application.name}


